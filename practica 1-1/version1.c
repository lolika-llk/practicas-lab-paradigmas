//identar y comentar
int i;//i almacena un caracter (su valor ascii)
main(){
    for(;i["]<i;++i){--i;}"];//for(;comarar i;accion"lo que esta en las siguientes lineas")
        read('-'-'-',i+++"hell\o, world!\n",'/'/'/'));/*se llama a la funcion read declarada debajo, los '-' equivalen a 0 y las '/' son 1*/
}
read(j,i,p){//se crea una funcion
    write(j/p+p,i---j,i/i);//funcion write de la biblioteca unistd
    //tomando la llamada de la funcion a write se le dan los argumentos "0/1+1"=0,"i-0"=i,"i/i"=1
}